spring:
  datasource:
    url: jdbc:mysql://localhost:3306/contact_book?serverTimezone=UTC
    username: root
    password: password 
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect   
  
